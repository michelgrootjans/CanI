@using CanI.Demo


<br />
<br />
<hr />
@if (User.Identity.IsAuthenticated)
{
    <text>Signed in as @User.Identity.Name</text>
    @Html.ActionLink("Sign out", "SignOut", "Account")
    <text> | You have the following roles: [@User.PrintRoles()]</text>
}
else
{
    @Html.ActionLink("Sign in...", "SignIn", "Account")
}
 | <small>If you want to change authorization rules, edit AbilityConfigurator.cs</small>
